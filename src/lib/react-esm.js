// ESM-compatible React entry point
import * as React from 'react/index.js';
export default React;
export const {
  createElement,
  Fragment,
  useState,
  useEffect,
  useContext,
  createContext,
  memo,
  forwardRef,
  // Add other React exports as needed
} = React;